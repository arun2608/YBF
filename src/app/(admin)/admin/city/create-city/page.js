import "bootstrap-icons/font/bootstrap-icons.css";
import CreateService from "./CreateCity";
import CreateState from "./CreateCity";
import CreateCity from "./CreateCity";

export const metadata = {
    title: 'Create City | Personal Secretary'
  }
  

const page = () => {
    return (
        <CreateCity/>
    )
}

export default page