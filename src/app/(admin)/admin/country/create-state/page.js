import "bootstrap-icons/font/bootstrap-icons.css";
import CreateService from "./CreateState";
import CreateState from "./CreateState";

export const metadata = {
    title: 'Create State | Personal Secretary'
  }
  

const page = () => {
    return (
        <CreateState/>
    )
}

export default page