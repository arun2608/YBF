import "bootstrap-icons/font/bootstrap-icons.css";
import CreateService from "./CreateDistrict";
import CreateState from "./CreateDistrict";

export const metadata = {
    title: 'Create State | Personal Secretary'
  }
  

const page = () => {
    return (
        <CreateState/>
    )
}

export default page